const { User } = require('../models');
// Is the about 'User' supposed to be upper or lower case?

const userController = {
// Create a new user


// Get all users


// Get a single user (by ID?)


// Update a user (by ID?)


// Delete a user (by ID?)


};

module.exports = userController;
